# ILovePrivacy — Required Libraries
# Install all of these by running:
#   pip install -r requirements.txt

flask>=3.0.0               # The web server
werkzeug>=3.0.0            # Handles file uploads safely (comes with Flask)
presidio-analyzer>=2.2.0   # Finds sensitive info in text
presidio-anonymizer>=2.2.0 # Replaces the sensitive info
pdfplumber>=0.10.0         # Reads text from PDFs
reportlab>=4.0.0           # Writes new PDFs
spacy>=3.7.0               # Language understanding (used by Presidio)
